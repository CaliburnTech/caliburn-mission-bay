// TODO: migrate to Supabase Storage

export const getPresignedUploadUrl = async (key, _contentType) => {
  console.log('TODO: migrate to Supabase Storage', { key });
  return { uploadUrl: '', publicUrl: '' };
};
